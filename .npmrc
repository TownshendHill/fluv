# Refuse to install on a Node or pnpm version this repo does not support,
# rather than printing a warning nobody reads. Angular 22 requires Node
# >=24.15.0, and the version floor is declared in package.json engines.
engine-strict=true
